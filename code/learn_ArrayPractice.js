// ready to practice - Basic Arrays
//

let heroPool = ['Lucy', 'Hannah', 'Curtis']

// console.log('The main hero is:  ' + heroPool[0]);

// adding a loop to the practice code
let x = heroPool.length
let y = 0
console.log(x);
while (y < x) {
    console.log(heroPool[y]);
    y++;
}
